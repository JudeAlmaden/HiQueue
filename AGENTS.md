@AGENTS.md
This application uses next.js version 16, app router, tailwindcss v4, biome. It does not use middleware and uses proxy-auth pattern instead.

read documentation in /docs folder: use the Design.md and Schema.md + any other relevant documentation to understand the codebase. 

use proper types not "unknown" and not "any"  and place it at "src/types" folder to avoid repetitive type definitions.

avoid lint errors and warnings, respect the existing code style and patterns.

improve existing code and performance and security, security is the highest priority.

if you need to create files, create them in the appropriate folders and with the appropriate names following the existing code style and patterns.

if you need to create folders, create them in the appropriate folders and with the appropriate names following the existing code style and patterns.

do not modify or delete existing files unless explicitly asked to do so.

do not reformat the code.

do not touch files that are not related to the task you are working on.

use proper commit messages, follow conventional commits.

do not create or update walkthrough artifacts unless explicitly requested by the user.