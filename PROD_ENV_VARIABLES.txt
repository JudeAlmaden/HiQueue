# Production Environment Variables for Vercel
# Copy these to Vercel Dashboard → Settings → Environment Variables

DATABASE_URL=postgresql://postgres.zbvfjaeghmjvkmdspmnh:spaghettiandmeatballs%232045@aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1
DIRECT_DATABASE_URL=postgresql://postgres:spaghettiandmeatballs%232045@db.zbvfjaeghmjvkmdspmnh.supabase.co:5432/postgres
AUTH_SECRET=your-super-secret-production-key-change-this
NEXTAUTH_URL=https://hi-queue.vercel.app
APP_NAME=HiQueue
NODE_ENV=production

# IMPORTANT NOTES:
# 1. Change AUTH_SECRET to a secure random string (generate with: openssl rand -base64 32)
# 2. DATABASE_URL uses port 6543 (Transaction mode) - fixes connection pool errors
# 3. connection_limit=1 prevents serverless connection pool exhaustion
# 4. trustHost: true in auth.config.ts allows all Vercel URLs without CORS issues
